# Optional — wire the contact form to Resend (https://resend.com).
# Without these set, /api/contact still validates and returns success,
# but submissions are only logged to the server console.
RESEND_API_KEY=
RESEND_FROM="Tim Brown Site <noreply@tangobrown.com>"
CONTACT_TO_EMAIL=tim@tangobrown.com
